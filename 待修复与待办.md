# 电商平台项目：待修复与待办

> 记录时间：2026-09-14
> 代码范围：`code-scaffold/`（Spring Boot 3.5.7 + Vue 2 + MySQL）
> 第一条来源：对**运行中的实例**做动态验证（发真实请求 + 查数据库），不是纸面推断。
> 每项都标了「怎么复现」和「怎么改」，改动时不用回头翻聊天记录。

**当前状态**：项目能正常启动运行（启动方式见文末附录 A）。下面是待办。

---

## 〇、2026-09-16 商户端改造进展（合并修复了以下历史项）

> 本次改造详情见 `商户端开发计划.md`；以下历史待办**已随本次改造修复**（待动态回归验证）：

| 历史项 | 状态 | 修复方式 |
| --- | --- | --- |
| P0-1 放行名单全放开 | ✅ | 收敛为 `/login /register /password /admin/login /merchant/login /merchant/register`；匿名浏览（商品/分类/轮播/文件下载）改 `@AuthAccess` 标注 |
| P0-2 JWT 只解码不验签 | ✅ | `JwtInterceptor` 全局密钥统一验签后写 request attribute；`TokenUtils` 只读 attribute，业务层不再 decode |
| P0-3 通过认证不校验角色 | ✅ | `/admin/**`→ADMIN、`/merchant/**`→MERCHANT、商品/分类/轮播写操作→ADMIN、订单/收藏→本人 |
| P0-4 `/admin/login` 死链路 | ✅ | 三角色统一为单一 `jwt.secret` + role 声明，密钥移入 `application.yml`，死链路变为正常入口 |
| P1-1 支付接口能造钱 | ✅ | `pay(id)` 重写：userId 取登录态、金额取服务端、校验归属与状态、`>=` 判断、加事务 |
| P1-2 下单金额可篡改 | ✅ | `save()`/`settle()` 服务端按 `goods.price` 重算，userId/手机号/地址取登录态 |
| P1-3 下单扣库存无事务 | ✅ | `settle()` 拆单版保持 `@Transactional` + `deductStore` 原子扣减；新增取消回补 `restoreStore` |
| Bug「添加人列永远空白」 | ✅ | 改为显示 `merchantName || adminName`（归属商户/录入人） |
| 5.2 订单状态流转（部分） | 🔶 | 状态字典 `OrderState` 已建；商户发货接口已上线；**前台「确认收货」还没做** |
| P2-4 InterceptorConfig 关闭自动配置 | ✅ | 改为 `implements WebMvcConfigurer` |

**仍未修复**：P0-5 密码明文存储、P1-4 其余 NPE 点（`CollectServiceImpl` 等）、P2-1 认证失败返回 HTTP 200、P2-2/P2-3、工程化待办。

> ⚠️ 存量库需要执行 `sql/migration_merchant.sql`（新库直接跑 `sql/code_scaffold.sql` 即可）。
> ⚠️ 全部旧 token 失效（密钥变了），所有端需要重新登录。
> 商户演示账号：`shop_demo / 123`（已审核通过）。

---

## 一、安全待修复（P0：建议优先做，都是小改动）

> P0 五项做完，能一次性堵掉下面第 1、2、3、6、7 条漏洞。

### P0-1 放行名单把全部业务接口都放开了

- [ ] 修复

**位置**：`springboot/src/main/java/com/example/springboot/common/InterceptorConfig.java:18-19`

```java
.excludePathPatterns("/login", "/register", "/password", "/admin/login",
        "/user/**", "/type/**", "/goods/**", "/carousel/**", "/collect/**", "/orders/**");
```

**实测证据**（未登录，不带任何 token）：

```bash
curl http://localhost:9999/user/selectAll     # → HTTP 200，返回全部用户【含明文密码】
curl http://localhost:9999/orders/selectAll   # → HTTP 200，返回全部订单
curl http://localhost:9999/collect/selectAll  # → HTTP 200
curl http://localhost:9999/admin/selectAll    # → 401（这条是唯一被拦住的）
```

**改法**：放行名单收敛到只留真正需要匿名的：

```java
.excludePathPatterns("/login", "/register", "/password", "/file/download/**");
```

### P0-2 JWT 只解码不验签，令牌可随意伪造

- [ ] 修复

**位置**：`springboot/src/main/java/com/example/springboot/utils/TokenUtils.java:96-107`

`getCurrentUser()` / `getCurrentAdmin()` 用的是 `JWT.decode(token)`，**只做 base64 解码，完全不校验签名**。而它们是 `OrdersServiceImpl.selectPage`、`GoodsServiceImpl.save`、`CollectServiceImpl.save/myCollect` 判断「当前用户是谁」的唯一依据。

**实测证据**——同一个伪造 token（`aud=1`，签名填垃圾字节），被两条代码路径区别对待：

| 请求 | 结果 |
| --- | --- |
| `/admin/selectAll`（走 `JwtInterceptor`，会 `verify`） | **401 拒绝** ✅ |
| `/collect/myCollect`（走 `TokenUtils`，只 `decode`） | **200，返回了 tom 的收藏** ❌ |
| `/orders/selectPage` | **200，返回全站 8 条订单**（涉及用户 2、4）❌ |

**改法**：先验签再取 claim，不要单独用 `JWT.decode`。建议在拦截器里验完签，把用户信息放进 `request.setAttribute(...)`，后续统一从那里取。

### P0-3 通过认证后从不检查角色

- [ ] 修复

**实测证据**：用**普通用户 tom 的合法 token**：

```bash
curl -H "token: <tom的token>" http://localhost:9999/admin/selectAll
# → 200，返回管理员列表【含明文密码】

curl -X POST http://localhost:9999/admin -H "token: <tom的token>" \
     -d '{"username":"zz_badadmin","password":"123"}'     # → 200，管理员创建成功
curl -X DELETE http://localhost:9999/admin/<id> -H "token: <tom的token>"  # → 200，删除成功
```

**改法**：`JwtInterceptor` 通过后按路径/注解校验角色。`/admin/**` 和商品/分类/轮播的写操作限 `ADMIN`；`/orders/**`、`/collect/**` 限本人。

### P0-4 删掉 `/admin/login` 那条坏掉的死链路

- [ ] 修复

**位置**：`AdminController.java:18-28`、`TokenUtils.java:25,48-54`、`common/LoginResponse.java`

**实测证据**：

```bash
curl -X POST http://localhost:9999/admin/login -d '{"username":"admin","password":"123"}'
# → 200，签发出 token
curl -H "token: <上面那个token>" http://localhost:9999/admin/selectAll
# → {"code":"401","token验证失败，请重新登录"}
```

原因：它用硬编码的 `ADMIN_TOKEN_SECRET = "hachimiAdminSecret2024"` 签名（**密钥明文写在源码里**），而 `JwtInterceptor:84` 用**用户密码**验签，两者永不匹配。

而且前端**从未调用过** `/admin/login`（已 grep 确认），`LoginResponse` 也只有这一处引用 —— 整条链路是死的。**建议直接删，不要修**。

> 顺带：管家的登录实际走的是 `WebController` 的 `/login`，用 `user` 表里 `role=ADMIN` 的记录（种子数据 id=1）。

### P0-5 密码明文存储，且被当作 JWT 密钥

- [ ] 修复

**位置**：`UserServiceImpl.java:50`（明文比对）、`UserServiceImpl.java:28,77`（硬编码默认密码 `123`）、`TokenUtils.java:38-43`（拿密码当 HMAC 密钥）

**实测**：`/user/selectAll` 与 `/admin/selectAll` 直接返回 `"password":"123"`。

**改法**：
1. 密码改 BCrypt 存储与比对（引入 `spring-security-crypto` 即可，不必上整个 Spring Security）
2. JWT 密钥移到配置文件/环境变量，不要用用户密码
3. `resetPassword`（`UserServiceImpl.java:69-79`）当前把密码重置成固定 `123`，入口 `/password` 又只需「用户名 + 手机号」——**手机号在第 1 条里能直接拖到，等于任意账号接管**。要加验证码或至少限流

---

## 二、安全待修复（P1：业务正确性）

### P1-1 支付接口可以「造钱」

- [ ] 修复

**位置**：`OrdersServiceImpl.java:103-119`

```java
User user = userMapper.selectById(orders.getUserId());   // userId 来自请求体
if (user.getAccount() > orders.getPrice()) {             // price 来自请求体
    orders.setState("已支付");
    ordersMapper.updateById(orders);
    user.setAccount(user.getAccount() - orders.getPrice());   // ← 负数即加钱
```

**实测证据**（未登录，不带 token）：

```bash
curl -X POST http://localhost:9999/orders/pay \
     -d '{"id":999999,"userId":8,"price":-1000000}'
# 余额 0.00 → 1000000.00
```

**注意：订单 id 是假的（999999 不存在），`updateById` 影响 0 行，余额照样变了** —— 不需要任何真实订单就能刷钱。

**改法**：
- `userId` 取自登录态，不信请求体
- `price` 服务端按 `goods.price * nums` 重算
- 加 `price > 0` 校验
- 校验订单归属与 `state`（当前**重复调用可以重复扣款**）
- `>` 应为 `>=`（余额恰好等于订单金额时会被误拒）
- 加 `@Transactional`

### P1-2 下单金额可篡改，且库存照扣

- [ ] 修复

**位置**：`OrdersServiceImpl.java:38-59`，前端对应 `vue/src/views/front/GoodsDetail.vue:110-120`

**实测证据**（未登录）：

```bash
curl -X POST http://localhost:9999/orders/add \
     -d '{"name":"cheat","goodsId":1,"price":0.01,"nums":1,"userId":9,...}'
# → 200
# 订单入库金额 = 0.01（商品1 真实单价 129.00）
# 商品1 库存 99→98、销量 87→88  ← 库存照样扣了
```

`price`、`nums`、`userId`、`userPhone`、`userAddress` 全部来自请求体，服务端从不按 `goods.price` 重算。

**改法**：`price` 服务端算；`userId` 取自登录态；`nums` 校验正整数且不超库存。

### P1-3 下单与扣库存没有事务，并发会超卖

- [ ] 修复

**位置**：`OrdersServiceImpl.java:39-58`

`goods.getStore()` 判断 → 插入订单 → `goods.setStore(store - nums)` 更新，是典型的「读-改-写」，且**没有 `@Transactional`**。

**改法**：加 `@Transactional`；库存改原子 SQL，靠数据库保证不超卖：

```sql
update goods set store = store - #{nums}, sales = sales + #{nums}
where id = #{id} and store >= #{nums}
```

然后判断影响行数，为 0 说明库存不足。

### P1-4 空指针导致的 500

- [ ] 修复

| 位置 | 问题 |
| --- | --- |
| `OrdersServiceImpl.java:91` | `currentUser.getRole().equals("USER")` —— token 无效时 NPE。**这是本项目唯一的行级隔离，一个空指针就绕过去了**（伪造 token 时 `getRole()` 返回 null） |
| `GoodsServiceImpl.java:41` | `TokenUtils.getCurrentUser().getId()` 未判空 |
| `CollectServiceImpl.java:39,93` | 同上 |
| `UserController.java:58` | `currentUser.getId()` 未判空 |
| `OrdersServiceImpl.java:41` | `goods` 为 null（goodsId 不存在）时 `goods.getStore()` NPE |

---

## 三、安全待修复（P2）

### P2-1 认证失败返回 HTTP 200

- [ ] 修复

**位置**：`exception/GlobalException.java:25-28`

**实测**：`/admin/selectAll` 无 token 时 → **HTTP 状态码 200**，body 里才是 `{"code":"401","token验证失败"}`。

**影响**：网关、WAF、监控、日志都看不出这是未授权请求。

**改法**：未认证返回真实 HTTP 401/403，业务错误才用 200 + body code。

### P2-2 文件上传/下载无防护（纵深防御缺失）

- [ ] 修复（优先级可放最后）

**位置**：`FileController.java:33-54`（上传）、`FileController.java:59-79`（下载）

**实测结论：路径穿越当前不可利用**（修正我先前的判断）：

| 尝试 | 结果 |
| --- | --- |
| `/file/download/..%5C..%5C..%5C..%5CWindows%5Cwin.ini` | HTTP **400** |
| `/file/download/..%2F..%2F..%2F..%2FWindows%2Fwin.ini` | HTTP **400** |
| 对照：不存在的文件名 | HTTP 200，0 字节 |

Tomcat 默认拒绝路径中的编码斜杠/反斜杠，在到达应用代码前就拦了。**但应用自身依然是零防护**（没有规范化、没有白名单），安全性完全依赖容器默认配置 —— 换容器或放宽 `relaxedPathChars` 立刻出问题。

**仍建议改**（成本很低）：

```java
File f = new File(ROOT_PATH, fileName).getCanonicalFile();
if (!f.toPath().startsWith(new File(ROOT_PATH).getCanonicalFile().toPath()))
    throw new ServiceException("非法路径");
```

**其他两点**：
- 上传不校验扩展名/MIME，配合 `Content-Disposition: inline`（第 65 行）可上传 `.html` 形成存储型 XSS
- `max-file-size: 200MB` 偏大

### P2-3 富文本 `v-html` 无过滤

- [ ] 修复

**位置**：`vue/src/views/front/GoodsDetail.vue:48`、`vue/src/views/manager/Goods.vue:124,159`

`goods.content` 直接 `v-html` 渲染。而 `/goods/update` 目前是匿名可调的（见 P0-1），外部可直接注入 `<img src=x onerror=...>`。

**改法**：引入 DOMPurify 做白名单过滤。

### P2-4 其他

- [ ] `CorsConfig.java` 第 1-13 行 import **重复粘贴了两遍**；`addAllowedOrigin("*")` 全放开，生产要换域名白名单
- [ ] `InterceptorConfig.java:9` `extends WebMvcConfigurationSupport` 会**关掉 Spring Boot MVC 自动配置**，应改 `implements WebMvcConfigurer`
- [ ] `UserController`、`TypeController`、`OrdersController` 上的 `@CrossOrigin` 与全局 `CorsFilter` 重复
- [ ] `application.yml:10-11` 数据库口令 `root/123456` 明文，应移到环境变量
- [ ] `OrdersServiceImpl.pay()` 里 `orders.setState("已支付")` 是**硬编码**的，请求体传什么 state 都会被覆盖 —— 这是刻意还是笔误需要确认
- [ ] 分页 `pageSize` 无上限，客户端可传 `999999` 一次拉全表

---

## 四、已知 Bug（不影响安全，但功能是坏的）

- [ ] **「添加人」列永远空白** —— `vue/src/views/manager/Goods.vue:31,138` 读的是 `userName`，而后端 `GoodsServiceImpl.java:93` 设的是 `adminName`（`Goods.java:32`）
- [ ] **「姓名」列永远空白** —— `vue/src/views/front/Orders.vue:25`、`manager/Orders.vue:26,87` 读 `userName`，但 `Orders.java:20` 里该字段已被注释掉
- [ ] **组件名写错** —— `Register.vue:43` 的 `name` 写成 `'Login'`；`Orders.vue:64`、`manager/Orders.vue:117` 写成 `"Type"`
- [ ] **收藏 toggle 并发不安全** —— `CollectServiceImpl.java:42-55` 先 `selectOne` 再 `delete`/`insert`，并发下撞唯一键 `uk_collect_user_goods` → 500
- [ ] **`order_item` 模块是半成品** —— 表建了、种子数据有、Mapper/Service/Controller 都写了，但 `OrdersServiceImpl.save` **从不写 `order_item`**，前端也没有任何地方调用 `/orderItem/**`。**这条和下面的购物车需求直接相关，建议合并做**
- [ ] **没有任何测试** —— `SpringbootApplicationTests.java` 只有空的 `contextLoads`

---

## 五、新功能需求

### 5.1 购物车

- [ ] 建表
- [ ] 后端接口
- [ ] 前端页面
- [ ] 与下单流程打通

**现状**：完全没有购物车。`vue/src/views/front/GoodsDetail.vue:110` 的「立即购买」直接构造订单提交，一次只能买一个商品、一件数量由页面上的 `el-input-number`（上限 10）决定。

**建议设计**：

新表 `cart`：

```sql
CREATE TABLE `cart` (
  `id` int NOT NULL AUTO_INCREMENT,
  `user_id` int NOT NULL COMMENT '用户ID',
  `goods_id` int NOT NULL COMMENT '商品ID',
  `nums` int NOT NULL DEFAULT 1 COMMENT '数量',
  `time` varchar(50) DEFAULT NULL COMMENT '加入时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_cart_user_goods` (`user_id`, `goods_id`),
  KEY `idx_cart_goods_id` (`goods_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='购物车表';
```

> 唯一键和现有 `collect` 表的 `uk_collect_user_goods` 是同一个思路，可以直接参考 `CollectServiceImpl.java` 的写法 —— 但**注意别抄它的并发问题**（见第四节）。

后端：仿照现有 `CollectController` 加 `CartController` + `ICartService` + `CartServiceImpl`（列表、加购、改数量、删除、清空）。

前端：
- 顶部导航加购物车入口 + 数量角标（`vue/src/conponents/Footer.vue` 或 `views/front/Front.vue` 的头部区域）
- 新增 `views/front/Cart.vue`：列表、改数量、删除、勾选、合计金额、结算
- `GoodsDetail.vue` 的「立即购买」旁边加「加入购物车」

**关键点：结算时要写 `order_item`**。现在这个表是空的没用起来（见第四节），而购物车一次结算多件商品，恰好就是 `order_item` 存在的意义。建议一起做：

- 结算 → 插入 1 条 `orders` + N 条 `order_item`（每件商品一条：`order_id` / `goods_id` / `goods_name` / `price` / `nums`）
- `orders` 的 `goods_id` / `name` / `price` 字段是单商品时代的遗留设计，多商品订单下应该只作为「主商品」或直接废弃，改由 `order_item` 承载明细 —— **这里要先定数据模型再动手**

**顺带要确认**：`orders.price` 语义是「订单总金额」还是「单价 × 数量」，现在代码里两种理解都有（`GoodsDetail.vue:114` 存的是 `price * num`）。

### 5.2 订单管理界面

- [ ] 前台：订单详情页
- [ ] 前台：按状态筛选 + 分页完善
- [ ] 后台：订单状态流转（发货）
- [ ] 前台：确认收货
- [ ] 统一订单状态字典

**现状盘点**（这两块都有页面，但功能很薄）：

| 位置 | 现在有什么 | 缺什么 |
| --- | --- | --- |
| `vue/src/views/front/Orders.vue` | 我的订单列表、取消支付、支付、删除 | 详情页、状态筛选、确认收货 |
| `vue/src/views/manager/Orders.vue` | 列表、详情抽屉（只读）、删除 | **改状态（发货）**、按状态/时间筛选 |

**状态字典问题**：现在只有三个状态，散落在前后端字符串字面量里：

```
待付款 / 已支付 / 已取消
```

缺「已发货 / 已完成 / 退款中 / 已退款」。而且判定逻辑分散：
- `front/Orders.vue:39-40` 用 `scope.row.state == '待付款'` 决定显示哪些按钮
- `OrdersServiceImpl.pay()` 硬编码写 `"已支付"`

**建议**：
1. 先定义统一的状态枚举（后端常量类 + 前端常量文件），别再散字符串
2. 状态流转规则明确：`待付款 →（支付）→ 已支付 →（发货）→ 已发货 →（确认）→ 已完成`，`待付款 →（取消）→ 已取消`
3. **状态变更必须走专用接口并校验权限**，不要像现在这样让前端把整个 row PUT 到 `/orders/update` —— 那等于让客户端随意改订单（`OrdersServiceImpl.update` 直接 `updateById`，无任何字段白名单）
4. 后台加发货操作，前台加确认收货

---

## 六、工程化待办

- [ ] **项目不是 git 仓库**，且没有 `.gitignore`，而目录下已经有 `node_modules/`、`dist/`、`target/`、`*.log`、`.idea/`、`.playwright-cli/`
      → 建议先 `git init` + 补 `.gitignore`（`node_modules/`、`target/`、`dist/`、`*.log`、`.idea/`、`.playwright-cli/`、`files/`）
- [ ] 补关键路径单测（下单、支付、库存；当前测试目录是空的）
- [ ] `GlobalException.java:41` 用 `e.printStackTrace()`，应上日志框架并保留 traceId

---

## 附录 A：怎么启动

在 `code-scaffold/` 目录下：

| 方式 | 操作 |
| --- | --- |
| 一键启动 | 双击 `start-all.bat`（先后端后前端） |
| 分开启动 | 双击 `start-backend.bat` / `start-frontend.bat` |
| IDEA | 直接跑 `SpringbootApplication`；前端另开终端 `npm run serve` |

- 前端 `http://localhost:8080`，后端 `http://localhost:9999`
- 账号：`admin / 123`（管理员）、`tom / 123`（普通用户），**登录页要选对角色**
- 依赖 MySQL 常开（库名 `bil_mall`，端口 3306）

**两个坑**：
1. 命令行直接敲 `mvn` 会失败，因为本机默认 JDK 是 **1.8** 而项目要 **17**。脚本已处理；手敲需 `JAVA_HOME="/c/Program Files/Java/jdk-17"`
2. `start-*.bat` 是 **GBK 编码**，**别用 VS Code 打开编辑**（会显示乱码，一保存就变 UTF-8 从而跑不起来）。用记事本，或先找我改成纯 ASCII

---

## 附录 B：复现上面安全问题的命令

改完之后用这些命令回归验证（`B=http://localhost:9999`）。

**P0-1 匿名访问**（修复后应全部 401）：

```bash
for u in /user/selectAll /orders/selectAll /collect/selectAll /admin/selectAll; do
  echo -n "$u => "; curl -s -o /dev/null -w "%{http_code}\n" "$B$u"
done
```

**P0-2 伪造 token**：造一个 `{"aud":"1"}` + 垃圾签名的 JWT：

```bash
# header/payload 直接 base64url，签名随便填
T='eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJhdWQiOiIxIiwiZXhwIjo5OTk5OTk5OTk5fQ.ZmFrZQ'
curl -H "token: $T" "$B/collect/myCollect"     # 修复后应 401，现在返回数据
```

**P0-3 普通用户越权**：用 `tom/123` 登录拿 token，然后：

```bash
curl -H "token: <tom的token>" "$B/admin/selectAll"          # 修复后应 403
curl -X DELETE -H "token: <tom的token>" "$B/admin/2"        # 修复后应 403
```

**P1-1 造钱**：

```bash
curl -X POST "$B/orders/pay" -H "Content-Type: application/json" \
     -d '{"id":999999,"userId":<你的测试账号id>,"price":-1000000}'
# 修复后应报错；现在余额会 +1000000
```

**P1-2 金额篡改**：

```bash
curl -X POST "$B/orders/add" -H "Content-Type: application/json" \
     -d '{"name":"cheat","goodsId":1,"price":0.01,"nums":1,"userId":<id>}'
# 修复后入库 price 应为 129.00（服务端按 goods.price 重算）
```

> ⚠️ 跑 P1 的用例会改库存和余额，**测完记得还原**（或干脆用专门的测试账号）。

---

## 附录 C：相关文档

- 项目问题知识库：`D:\workspace\AI-Fullstack-Knowledge`
  - `01-开发环境/Maven编译SpringBoot3项目报类文件版本错误.md`（附录 A 的坑 1）
  - `01-开发环境/cmd批处理脚本换行与编码导致解析错乱.md`（附录 A 的坑 2）
  - `01-开发环境/GitBash传中文参数给curl变成GBK导致接口500.md`（**用 curl 测中文接口时会踩**，别误判成后端 bug）
  - `04-前端开发/VueRouterhistory模式刷新页面404.md`
